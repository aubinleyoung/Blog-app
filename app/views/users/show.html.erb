<main>
<h1 class="title">This is single user</h1>
<ul class="users-list">
  <li class="user-details">
    <div class="image-box">
    
    </div>
    <div class="details-box">
      <h2><%= @user.name %></h2>
      <p>Number of posts: <%= @user.post_counter %></p>
    </div>
  </li>
  <li class="li-box">
    <div class="inner-li-box">
      <h2>Bio</h2>
      <p class="p_tags"> <%= @user.bio %> </p>
    </div>
  </li>
  <li>
    <h2 class="single-header">Posts</h2>
    <% if @user.three_recent_posts.blank? %>
      <div class="li-box">
        <p class="p_tags inner-li-box">There are no posts available.</p>
      </div>
    <% else %>
      <ul class="posts-box">
        <% @user.three_recent_posts.each do |post| %>
        <a href="<%= url_for("/users/#{@user.id}/posts/#{post.id}") %>" >
          <li class="li-box">
            <div class="inner-li-box">
              <h3>Post Title: <%= post.title %></h3>
              <p class="p_tags"><%= post.content %></p>
              <div class="comments_n_likes">
                <p>Comments: <%= post.comments_counter %>, Likes: <%= post.likes_counter %></p>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
      </a>
      <div class="button_div">
        <a href="<%= url_for(user_posts_path(@user.id)) %>" class="button">See all posts</a>
      </div>
    <% end %>
  </li>
  <li class="li-box">

  <a href="<%= url_for('/users/user_path/posts/new') %>" class="button button-wide">Create new post</a>
</li>
</ul>
</main>

